import { Route } from '@angular/router';
import { DropsPageComponent } from './drops-page/drops-page.component';

export const appRoutes: Route[] = [
    {
        path: '',
        component: DropsPageComponent
    }
];
